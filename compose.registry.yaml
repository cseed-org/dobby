# Optional: pull a CI-tested multi-architecture image instead of building locally.
services:
  bot:
    image: "${DOBBY_IMAGE:?Set DOBBY_IMAGE in .env to ghcr.io/owner/repository:main}"
    pull_policy: always
